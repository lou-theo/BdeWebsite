{% extends 'base.html.twig' %}

{% block body %}
    <h3>Formulaire d'annonce</h3>

    <div class="well">
        {#
        {{ form_start(form) }}
        {{ form_row(form.mail) }}
        {{ form_row(form.title) }}
        {{ form_row(form.message) }}
        {{ form_end(form) }}
        #}
        TEST
    </div>
{% endblock %}